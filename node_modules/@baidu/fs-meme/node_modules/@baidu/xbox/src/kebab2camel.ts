/**
 * @file 把 kebab case 字符串转换成 camel case
 * @author wangyongqing <wangyongqing01@baidu.com>
 */

/**
 * 把 kebab case 字符串转换成 camel case
 *
 * @param source 源字符串
 * @return 返回处理后的字符串
 * @example
 * kebab2camel('abc-acd-e-f'); // abcAcdEF
 */
export function kebab2camel(source: string) {
    return String(source)
        .toLowerCase()
        .replace(/-+(.)/g, (_match, alpha) => alpha.toUpperCase());
}
