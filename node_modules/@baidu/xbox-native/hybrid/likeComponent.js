/**
 * @file likeComponent
 * @module data
 * @author panming <panming0@baidu.com>
 */

import {getParams} from '../utils';
import register from '../register';
import hybrid from './index';
register('easybrowse/likeComponent', {
    v: 24,
    versionRange: ['10.6'],
    filter() {
        return {
            module: 'easybrowse',
            action: 'likeComponent'
        };
    }
});
/**
 * likeComponent
 * @function likeComponent
 */
export default data => hybrid(getParams('easybrowse-likeComponent', data));
