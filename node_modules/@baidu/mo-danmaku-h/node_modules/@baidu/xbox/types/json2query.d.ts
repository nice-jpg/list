/**
 * @file 将对象转成url，但是没有添加对数组支持
 * @author wangyongqing <wangyongqing01@baidu.com>
 */
import type { KeyValue } from './is';
export declare function mapArray(args: unknown[]): unknown[];
/**
 * 将对象转成url，但是没有添加对数组、对象支持，直接 JSON.stringify 输出
 *
 * @param json 待处理的json对象
 * @param [encode = false] 是否 encodeURI 对象/数组字符串
 * @return 处理之后的结果
 * @example
 * json2query({
 *       a: 1,
 *       b: 'string'
 *  });
 * //'a=1&b=string'
 * json2query({
 *       a: 1,
 *       b: 'string',
 *       c: [1, 2, 3]
 *   });
 * //'a=1&b=string&c=[1,2,3]'
 * json2query({
 *       a: 1,
 *       b: 'string',
 *       c: [1, 2, 3]
 *   }, true);
 * //'a=1&b=string&c=%5B1%2C2%2C3%5D'
 */
export declare function json2query(json: string | KeyValue, encode?: boolean): string;
