"use strict";
/**
 * @file: class 类名处理
 * @author: xulinfeng@baidu.com
 * @date:   2019-11-27 18:38:45
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.removeClass = exports.addClass = exports.hasClass = exports.trim = void 0;
/**
 * 去除字符串前后空格
 *
 * @param str 源字符串
 */
function trim(str) {
    return str.replace(/(^\s*)|(\s*$)/g, '');
}
exports.trim = trim;
/**
 * 元素是否包含类名
 *
 * @param obj 元素
 * @param cls 类名
 */
function hasClass(obj, cls) {
    return new RegExp("(s|^)" + cls + "(s|$)").test(obj.className);
}
exports.hasClass = hasClass;
/**
 * 元素添加类名
 *
 * @param obj 元素
 * @param cls 类名
 */
function addClass(obj, cls) {
    if (!hasClass(obj, cls)) {
        obj.className = trim(obj.className) + ' ' + cls;
    }
    return obj;
}
exports.addClass = addClass;
/**
 * 元素移除类名
 *
 * @param obj 元素
 * @param cls 类名
 * @return 元素对象
 */
function removeClass(obj, cls) {
    if (hasClass(obj, cls)) {
        var reg = new RegExp("(s|^)" + cls + "(s|$)");
        obj.className = trim(obj.className.replace(reg, ' '));
    }
    return obj;
}
exports.removeClass = removeClass;
