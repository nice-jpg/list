/**
 * @file 请求库封装
 * @author zhangsiyuan(zhangsiyuan@baidu.com)
 */

import Fly from 'flyio/dist/npm/fly';

const request = new Fly();
// 5秒超时时间
request.config.timeout = 3000;
request.config.withCredentials = true;
// 请求拦截器
// request.interceptors.request.use(async request => {
//     const customBody = JSON.parse(JSON.stringify(request.body || {}));
//
// });
//
// // response拦截器
// request.interceptors.response.use(
//     res => {
//
//     },
//     err => {
//
//     });

export {
    request,
    Fly as Request
};
