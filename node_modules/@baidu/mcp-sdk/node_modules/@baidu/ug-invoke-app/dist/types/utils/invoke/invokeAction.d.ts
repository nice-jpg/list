import { AppName } from '../../config/app/appConfigMap';
/**
 *
 * invokeAction包含了
 * 直接使用location = scheme
 * 和iframe两种方式
 *
 */
export declare function invokeAction(scheme: string, appName: AppName): void;
