/**
 * @file 对微信开放标签支持的检测
 * @author zhangsiyuan(zhangsiyuan@baidu.com)
 */
import {getHostInfo} from './getHostInfo';
import {WxTagConfig} from '../../api/createWxTag';
import {AppConfig} from '../../config/app/appConfigMap';

export function supportWxTag(options: WxTagConfig, appConfig: AppConfig): boolean {
    const hasAppid: boolean = !!appConfig.wxAppid;
    const isWechat: boolean = getHostInfo().browser === 'wechat';
    return hasAppid && isWechat && !!options.wxInfo.appId;
}
