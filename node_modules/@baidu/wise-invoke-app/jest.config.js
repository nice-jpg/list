/**
 * @file 测试配置
 * @author xiongyang(xiongyang@baidu.com)
 */

module.exports = {
    rootDir: './',
    testRegex: './test/unit/.*\.spec\.js',
    collectCoverage: true,
    verbose: true,
    collectCoverageFrom: [
        '<rootDir>/src/**/*.js'
    ],
    // 需要transform的es模块，需要在这里声明
    transformIgnorePatterns: [
        '<rootDir>/node_modules/(?!(@baidu))'
    ]
};
