/**
 * @file types.ts
 * @author leizhihao
 */

import {JumpProps} from '@baidu/auc-base/types/jumpProps';
import {LogExtraParams} from "@baidu/auc-base/types/logExtraParams";

export interface AucBigImageTextData {
    styles?: object;
    visited?: boolean;
    logBaseType?: string;

    title: string;
    labelProp: object;
    widgetItems: Array<object>;
    imageProps: object;
    isLive: boolean;
    isPlayBack: boolean;
    isPreload: boolean;
    jumpProps: JumpProps;
    logExtraParams: LogExtraParams;
    isActive: boolean;
    videoSrc: string;
}
