export default class Fullscreen {
    constructor(api, pluginOptions) {
        this.api = api;
        this.pluginOptions = pluginOptions;
    }
    /**
     * 是否全屏态，用户只可读取该属性
     * run为本插件对应的方法实际执行的函数
     */
    get() {
        return !!this.api.getPlayerOption('fullscreen');
    }
}

