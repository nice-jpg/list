
export interface UbcType {
    textMode: boolean;
    page: string;
    source: string | number;
    from: string | number;
    value: string | number;
    type: string | number;
    timingId: string | number;
    ext: any;
}

// init ubcId
export interface ConfigType {
    event: string; // 用于ubc的标识，一个app中不重复即可
    ubcId: string; // ubcId
    params: UbcType;
}
