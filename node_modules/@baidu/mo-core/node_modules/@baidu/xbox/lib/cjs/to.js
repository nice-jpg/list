"use strict";
/**
 * @file 类型转换
 * @author wangyongqing <wangyongqing01@baidu.com>
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.toString = exports.toBoolean = exports.toNumber = exports.toArray = void 0;
var is_1 = require("./is");
/**
 * 将传入的对象转换成数组
 *
 * @param arrayLike 被转对象
 * @return 返回array
 */
function toArray(arrayLike) {
    return [].slice.call(arrayLike);
}
exports.toArray = toArray;
/**
 * 将传入的对象转换成数字
 *
 * @param value 被转对象
 * @param [defaultValue=null] 如果不能转，默认返回值
 * @return 返回 number
 */
function toNumber(value, defaultValue) {
    if (defaultValue === void 0) { defaultValue = null; }
    if ((0, is_1.isNil)(value)) {
        return defaultValue;
    }
    return Number(value);
}
exports.toNumber = toNumber;
/**
 * 将传入的对象返回布尔
 *
 * @param value 被转对象
 * @param [defaultValue=false] 如果不能转，默认返回值
 * @return 返回boolean
 */
function toBoolean(value, defaultValue) {
    if (defaultValue === void 0) { defaultValue = false; }
    if ((0, is_1.isNil)(value)) {
        return defaultValue;
    }
    return Boolean(value);
}
exports.toBoolean = toBoolean;
/**
 * 将传入对象返回字符串
 *
 * @function toString
 * @param value 被转对象
 * @param [defaultValue=null] 如果不能转，默认返回值
 * @return 返回字符串
 */
function toString(value, defaultValue) {
    if (defaultValue === void 0) { defaultValue = null; }
    if ((0, is_1.isNil)(value)) {
        return defaultValue;
    }
    if ((0, is_1.isString)(value)) {
        return value;
    }
    return String(value);
}
exports.toString = toString;
