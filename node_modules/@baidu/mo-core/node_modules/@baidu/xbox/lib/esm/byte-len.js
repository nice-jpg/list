/**
 * @file bytelen
 * @author wangyongqing <wangyongqing01@baidu.com>
 */
/**
 * 计算字符串长度，中文及全角字符等算作两个字节长度
 * @param subject 字符串
 * @example
 * ```
 * byteLen('中国'); // 4
 * ```
 */
export function byteLen(subject) {
    return String(subject).replace(/[^\x00-\xff]/gi, '**').length;
}
