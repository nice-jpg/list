export interface aucStarHeaderTypes {
    styles: object;
    auhomepageurl: string; // 跳转url
    auhomepagecmd: object; // 头部跳转cmd
    auimage: string; // 头像imgurl
    auimageflag: string; // v标url
    auname: string; // 名称
    auabs: string; // 简介
    vip: string;
    aumrid: string;
    followtype: string;
    isVisited: boolean;
    isFollow: boolean;
    isLoading: boolean;
}