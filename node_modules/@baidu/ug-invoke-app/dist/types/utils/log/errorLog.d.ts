/**
 * @file send error
 * @author zhangsiyuan(zhangsiyuan@baidu.com)
 */
import { InnerInvokeConfig } from '../../api/invokeApp';
import { Status } from '../../config/Status';
export declare function getRandomNumber(): number;
export declare function isHit(statRatio: number): boolean;
/**
 *
 *
 * ug-invoke-app行为统计打点
 * 尝试调起
 * 异常报错
 * 调起成功、失败、应用商店等
 * status必须传，其他的会根据invokeapp第一次进入的时候的缓存发出
 * 如果有单次覆盖参数，可以在这个方法里传，不影响缓存
 *
 *
 */
export declare function sendH5ErrLog(options: InnerInvokeConfig, status: Status, useSendBeacon?: boolean): void;
