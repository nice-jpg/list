import { Program, Node } from 'estree';
import { JSComponentInfo } from '../models/component-info';
import { JSSanSourceFile } from '../models/san-source-file';
/**
 * 把包含 San 组件定义的 JavaScript 源码，通过静态分析（AST），得到组件信息。
 */
export declare class JavaScriptSanParser {
    private readonly filePath;
    root: Program;
    componentInfos: JSComponentInfo[];
    entryComponentInfo?: JSComponentInfo;
    private sanComponentIdentifier?;
    private defineComponentIdentifier?;
    private defaultExport?;
    private imports;
    private exports;
    private componentIDs;
    private defaultPlaceholderComponent?;
    private id;
    constructor(filePath: string, fileContent?: string, sourceType?: 'module' | 'script');
    parse(): JSSanSourceFile;
    parseComponents(): [JSComponentInfo[], JSComponentInfo | undefined];
    wireChildComponents(): void;
    private createChildComponentReference;
    private parseComponentFromNode;
    /**
     * 解析文件中出现的名字：找到重要的类名、方法名以及它们的来源
     */
    parseNames(): void;
    parseImportedNames(): Generator<[string, string, string]>;
    createComponent(node: Node, name?: string, isDefault?: boolean): JSComponentInfo;
    private getOrCreateDefaultLoaderComponent;
    private getPropertiesFromComponentDeclaration;
    private isComponent;
    private isDefineComponentCall;
    private isCreateComponentLoaderCall;
    private isComponentClass;
    private isImportedFromSan;
    private isImportedFrom;
    private stringify;
}
