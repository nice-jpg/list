import { ComponentInfo } from '../models/component-info';
import { JSEmitter } from '../target-js/js-emitter';
export declare const RENDERER_ARGS: string[];
/**
 * Each ComponentClass is compiled to a render function
 */
export declare class RendererCompiler {
    private ssrOnly;
    emitter: JSEmitter;
    constructor(ssrOnly: boolean, emitter?: JSEmitter);
    /**
     * 把 ComponentInfo 编译成 Render JS 匿名函数源码
     */
    compileComponentRendererSource(componentInfo: ComponentInfo): string;
    compileComponentRendererBody(info: ComponentInfo): string;
    emitInitDataInCompileTime(initData: any): void;
    emitInitDataInRuntime(): void;
    /**
    * 生成组件 renderer 时 ctx 对象构建的代码
    */
    private genComponentContextCode;
}
