import { JSEmitter } from '../target-js/js-emitter';
import { ANodeCompiler } from './anode-compiler';
import { ANode } from 'san';
/**
 * 编译一个 HTML 标签
 *
 * 每个 ElementCompiler 对象对应于一个 ComponentClass，可以用来编译该组件中的所有 HTML 标签，并递归地调用 ANodeCompiler 来编译标签内容。
 */
export declare class ElementCompiler {
    private aNodeCompiler;
    private emitter;
    /**
     * @param aNodeCompiler 编译 aNode 的对象，编译标签内容时用
     * @param emitter 代码输出器，产出代码塞到这里面
     */
    constructor(aNodeCompiler: ANodeCompiler<never>, emitter?: JSEmitter);
    /**
     * 编译元素标签头
     */
    tagStart(aNode: ANode): void;
    private compileProperty;
    private isLiteral;
    private compileBindProperties;
    /**
     * 编译元素闭合
     */
    tagEnd(aNode: ANode): void;
    /**
     * 编译元素内容
     */
    inner(aNode: ANode): void;
}
