import { _ } from './underscore';
import { createResolver } from './resolver';
import { SanData } from './san-data';
import { JSEmitter } from '../target-js/js-emitter';
export interface SanSSRHelpers {
    /**
     * 无状态的工具库，类似 lodash
     */
    _: typeof _;
    /**
     * SanData 的 SSR 运行时替代品
     */
    SanData: typeof SanData;
    /**
     * 组件 render、Class 解析器
     */
    createResolver: typeof createResolver;
}
/**
 * 产出运行时代码
 */
export declare function emitHelpersAsIIFE(emitter: JSEmitter): void;
/**
 * 产出运行时代码
 */
export declare function emitHelpers(emitter: JSEmitter): void;
/**
 * 编译成 render 函数时，使用的 helper
 */
export declare function createHelpers(): SanSSRHelpers;
