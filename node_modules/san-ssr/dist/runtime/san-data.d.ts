interface DataObject {
    [key: string]: any;
}
interface Computed {
    [k: string]: (this: {
        data: SanData;
    }) => any;
}
/**
 * SSR 期间的 Data 实现，替代 import('san').SanData
 *
 * * 不涉及视图更新
 * * 便于编译期优化
 */
export declare class SanData {
    data: DataObject;
    computed: Computed;
    constructor(data: DataObject, computed?: Computed);
    get(path: string): any;
    set(path: string, value: string): string | null;
    removeAt(path: string, index: number): void;
    parseExpr(expr: string): string[];
}
export {};
