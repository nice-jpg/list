import { ANode, ANodeProperty } from 'san';
/**
* 获取 ANode props 数组中相应 name 的项
*/
export declare function getANodePropByName(aNode: ANode, name: string): ANodeProperty | undefined;
/**
 * 获取 ANode 的 props
 *
 * 做了一点归一化：对于布尔属性，只要 key 存在就把它的值设为 true
 */
export declare function parseANodeProps(aNode: ANode): ANodeProperty[];
/**
 * 先序遍历 ANode 树
 */
export declare function visitANodeRecursively(aNode: ANode, visitor: (aNode: ANode) => void): void;
