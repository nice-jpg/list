"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    Object.defineProperty(o, k2, { enumerable: true, get: function() { return m[k]; } });
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.visitANodeRecursively = exports.parseANodeProps = exports.getANodePropByName = void 0;
const TypeGuards = __importStar(require("./type-guards"));
const san_1 = require("san");
/**
* 获取 ANode props 数组中相应 name 的项
*/
function getANodePropByName(aNode, name) {
    for (const prop of aNode.props) {
        if (prop.name === name)
            return prop;
    }
}
exports.getANodePropByName = getANodePropByName;
/**
 * 获取 ANode 的 props
 *
 * 做了一点归一化：对于布尔属性，只要 key 存在就把它的值设为 true
 */
function parseANodeProps(aNode) {
    return aNode.props.map(prop => {
        if ((TypeGuards.isExprTextNode(prop.expr) ||
            TypeGuards.isExprStringNode(prop.expr)) && prop.noValue) {
            prop.expr = {
                type: san_1.ExprType.BOOL,
                value: true
            };
        }
        return prop;
    });
}
exports.parseANodeProps = parseANodeProps;
/**
 * 先序遍历 ANode 树
 */
function visitANodeRecursively(aNode, visitor) {
    visitor(aNode);
    for (const child of aNode.children || [])
        visitANodeRecursively(child, visitor);
}
exports.visitANodeRecursively = visitANodeRecursively;
//# sourceMappingURL=anode-util.js.map