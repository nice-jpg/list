import { JSEmitter } from '../js-emitter';
import { DynamicComponentInfo } from '../../models/component-info';
export declare class ComponentClassCompiler {
    emitter: JSEmitter;
    constructor(emitter?: JSEmitter);
    /**
     * 编译一个 ComponentClass 的 proto。例如输入组件类：
     *
     * class Foo { computed = { author: () => 'harttle' } }
     *
     * 输出字符串：
     *
     * computed: {
     *     author: () => 'harttle'
     * },
     */
    compile(componentInfo: DynamicComponentInfo): void;
    private emitObjectLiteral;
    private emitArray;
    private emitMethod;
}
