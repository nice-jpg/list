module.exports = function () {
  /**
  * @file inject.js
  * @author renzhonghua@baidu.com
  * @description 鉴权接口
  */
  var invoke = require("boxjs/dist/core/invoke");

  var $ = require('../utils/_bdboxWrapper');

  return function () {
    var action = 'feed/injectJS';
    var data = {};

    if ($.isIOS) {
      data = {
        files: ['inject:boxjs/inject.js'],
        name: 'injectjs'
      };
    }

    return invoke(action, data);
  };
}();